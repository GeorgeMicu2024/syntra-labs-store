'use client';
import { useMemo,useState } from 'react'; import { products } from '@/lib/products'; import ProductCard from './ProductCard'; import { useCart } from './CartProvider';
const cats=['All','Metabolic','Peptide','Cellular','Hormone','Lab Supplies'];
export default function ShopClient(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const {items,total,setQty,remove}=useCart();const list=useMemo(()=>products.filter(p=>(cat==='All'||p.category===cat)&&(`${p.name} ${p.code} ${p.strength}`.toLowerCase().includes(q.toLowerCase()))),[q,cat]);
async function checkout(){const res=await fetch('/api/checkout',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items:items.map(i=>({id:i.product.id,quantity:i.quantity}))})});const data=await res.json();if(data.url)location.href=data.url;else alert(data.error||'Checkout unavailable');}
return <><section className="shop-tools"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search name, strength or code"/><div>{cats.map(c=><button className={cat===c?'active':''} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div><b>{list.length} PRODUCTS</b></section><section className="product-grid">{list.map(p=><ProductCard key={p.id} product={p}/>)}</section>
<section id="cart" className="cart-panel"><div><span className="kicker">YOUR ORDER</span><h2>Shopping cart</h2></div>{items.length===0?<p>Your cart is empty.</p>:<><div className="cart-items">{items.map(i=><div key={i.product.id}><img src={i.product.image} alt=""/><span><b>{i.product.name}</b><small>{i.product.strength}</small></span><input type="number" min="1" value={i.quantity} onChange={e=>setQty(i.product.id,Number(e.target.value))}/><strong>£{(i.product.price*i.quantity).toFixed(2)}</strong><button onClick={()=>remove(i.product.id)}>×</button></div>)}</div><div className="cart-total"><span>Total</span><b>£{total.toFixed(2)}</b><button onClick={checkout}>Secure checkout</button></div></>}</section></>}
