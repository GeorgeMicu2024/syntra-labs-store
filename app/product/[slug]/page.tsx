import { notFound } from 'next/navigation'; import Image from 'next/image'; import { getProduct,products } from '@/lib/products'; import AddButton from './AddButton';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)notFound();return <section className="product-page"><div className="product-visual"><Image src={p.image} alt={`${p.name} ${p.strength}`} width={900} height={900}/></div><div className="product-info"><span className="kicker">{p.category} · {p.code}</span><h1>{p.name}</h1><div className="strength">{p.strength}</div><p>{p.description}</p><div className="price">£{p.price.toFixed(2)}</div><AddButton product={p}/><div className="specs"><div><b>Format</b><span>Single research vial</span></div><div><b>Availability</b><span>{p.stock>0?'In stock':'Out of stock'}</span></div><div><b>Reference</b><span>SL-{p.code}</span></div><div><b>Use</b><span>Laboratory research only</span></div></div><div className="warning"><b>Important:</b> Not for human or veterinary use. This page does not provide medical advice, dosing information or therapeutic claims.</div></div></section>}
